[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    asyncio: async tests (pytest-asyncio)
    airgap: runs only inside the airgapped container (make verify-airgap)
